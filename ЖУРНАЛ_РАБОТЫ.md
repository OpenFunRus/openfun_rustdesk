# 📝 Журнал работы над проектом RustDesk

## 🗓️ 16 января 2026

### 1. Форк репозитория
- Открыл https://github.com/rustdesk/rustdesk
- Нажал **Fork**
- Создал: https://github.com/OpenFunRus/openfun_rustdesk

### 2. Удаление `.gitmodules` на GitHub
- Перешёл в форк на GitHub
- Удалил файл `.gitmodules` через веб-интерфейс
- Commit: "Delete .gitmodules"

### 3. Скачивание проекта
- Скачал ZIP архив форка
- Распаковал в `D:\Cursor\rustdesk-1.4.5\`

### 4. Копирование файлов документации
- Скопировал `КАСТОМИЗАЦИЯ.md` в корень проекта
- Скопировал `build-x86-sciter-only.yml` в `.github/workflows/`

### 5. Скачивание `hbb_common`
```bash
cd D:\Cursor\rustdesk-1.4.5\libs
git clone https://github.com/rustdesk/hbb_common.git hbb_common_temp
Remove-Item -Recurse -Force hbb_common
Move-Item hbb_common_temp hbb_common
```

### 6. Удаление `.git` из `hbb_common`
```bash
cd D:\Cursor\rustdesk-1.4.5\libs\hbb_common
Remove-Item -Recurse -Force .git
```
**Результат:** `hbb_common` теперь обычная папка (31 файл), не submodule

### 7. Добавление изменений в Git
```bash
cd D:\Cursor\rustdesk-1.4.5
git add .
git commit -m "Update: Fresh fork with all customizations and hbb_common as regular folder"
```

### 8. Push на GitHub
```bash
git push -f origin master
```
**Результат:** Все файлы загружены на https://github.com/OpenFunRus/openfun_rustdesk

### 9. Создание журнала работы
- Создал `ЖУРНАЛ_РАБОТЫ.md`
- Загрузил на GitHub

---

## 📋 Текущее состояние

### ✅ Что загружено:
- `libs/hbb_common/` - 31 файл (обычная папка, не submodule)
- `КАСТОМИЗАЦИЯ.md` - полная документация по изменениям
- `.github/workflows/build-x86-sciter-only.yml` - workflow для сборки
- `ЖУРНАЛ_РАБОТЫ.md` - этот файл

### ✅ Изменённые файлы (из КАСТОМИЗАЦИЯ.md):
- `src/ui/index.tis` - минималистичный UI
- `src/ui/index.css` - Tabler.io Dark Theme
- `src/common.rs` - приоритет ENV для сервера/ключ/API
- `src/ipc.rs` - блокировка записи защищённых настроек

---

### 10. Исправление ошибки сборки
**Проблема:** Ошибка компиляции - отсутствует `libs/portable/src/res/label.png`
```
error: couldn't read libs\portable\src\./res/label.png: 
The system cannot find the file specified. (os error 2)
```

**Причина:** Файл существует локально, но не был добавлен в Git

**Решение:**
```bash
cd D:\Cursor\rustdesk-1.4.5
git add -f libs/portable/src/res/
git commit -m "Fix build: Add missing label.png resource file"
git push origin master
```

**Результат:** ✅ Файл добавлен, ошибка исправлена

### 11. Первая успешная компиляция
- Запустил workflow **"Build Only x86 Sciter Version"** на GitHub Actions
- Перешёл: https://github.com/OpenFunRus/openfun_rustdesk/actions
- Нажал **"Run workflow"**
- **Результат:** ✅ Сборка завершилась успешно!

### 12. Настройка GitHub Secrets
Добавил секреты в репозиторий:
- Перешёл: **Settings** → **Secrets and variables** → **Actions**
- Нажал **"New repository secret"**

Добавленные секреты:
1. `RENDEZVOUS_SERVER` - адрес сервера (например: `85.113.41.100:21116`)
2. `RS_PUB_KEY` - публичный ключ сервера
3. `API_SERVER` - адрес API (например: `https://85.113.41.100`)
4. `RS_PASSWORD` - статичный пароль для кассиров

**Результат:** ✅ Все 4 секрета добавлены

### 13. Повторная компиляция с секретами
- Запустил workflow заново: **"Run workflow"**
- Секреты добавлены, но **код еще не изменен** для их использования
- **Ожидаю результат:** Обычная сборка (ENV пока не используется)

### 14. Внедрение защиты через HARDCODED константы
**Задача:** Жёстко зашить настройки в код, чтобы нельзя было изменить

**Изменённые файлы:**

#### `libs/hbb_common/src/config.rs`
Добавлены immutable константы (читаются при компиляции):
```rust
pub const HARDCODED_RENDEZVOUS_SERVER: &str = option_env!("RENDEZVOUS_SERVER").unwrap_or("");
pub const HARDCODED_KEY: &str = option_env!("RS_PUB_KEY").unwrap_or("");
pub const HARDCODED_API_SERVER: &str = option_env!("API_SERVER").unwrap_or("");
pub const HARDCODED_PASSWORD: &str = option_env!("RS_PASSWORD").unwrap_or("");
```

#### `src/common.rs`
Изменён приоритет в 3 функциях:
1. `get_custom_rendezvous_server()` - HARDCODED проверяется ПЕРВЫМ
2. `get_key()` - HARDCODED проверяется ПЕРВЫМ
3. `get_api_server_()` - HARDCODED проверяется ПЕРВЫМ

#### `src/ipc.rs`
Добавлена защита от изменения:
1. `get_permanent_password()` - возвращает HARDCODED если установлен
2. `set_permanent_password()` - блокирует сохранение если HARDCODED
3. `set_options()` - фильтрует и НЕ сохраняет защищённые настройки

**Результат:** 
- ✅ Если настройки зашиты через GitHub Secrets - их НЕЛЬЗЯ изменить
- ✅ .toml файлы игнорируются
- ✅ EXE имя игнорируется
- ✅ Настройки UI игнорируются
- ✅ Защищённые настройки НЕ сохраняются в файлы

---

## 🚀 Следующие действия

_(здесь будут записываться новые действия)_

---
