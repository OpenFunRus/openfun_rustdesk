# 🎨 Документация по кастомизации RustDesk

## 📋 Содержание
1. [Общее описание](#общее-описание)
2. [UI и дизайн (Tabler.io Dark Theme)](#ui-и-дизайн)
3. [Безопасность (защита настроек сервера)](#безопасность)
4. [GitHub Actions (сборка с кастомным сервером)](#github-actions)
5. [Настройка GitHub Secrets](#настройка-github-secrets)
6. [Использование](#использование)

---

## 📝 Общее описание

**Цель:** Создать кастомную версию RustDesk для кассиров с:
- ✅ Минималистичным UI в тёмной теме Tabler.io
- ✅ Размером окна 320x200 пикселей
- ✅ Зашитыми настройками сервера И пароля (невозможно изменить)
- ✅ Возможностью менять **только ID** через `RustDesk.toml`

**Что защищено:**
- ❌ Кассиры НЕ могут менять сервер (зашито в EXE)
- ❌ Кассиры НЕ могут менять ключ (зашито в EXE)
- ❌ Кассиры НЕ могут менять API (зашито в EXE)
- ❌ Кассиры НЕ могут менять пароль (зашито в EXE)
- ❌ Настройки сервера НЕ попадают в `.toml` файлы
- ✅ Кассиры МОГУТ менять только ID через `RustDesk.toml`

---

## 🎨 UI и дизайн

### Файл 1: `src/ui/index.tis`

#### Изменение 1.1: Правильная структура render() с input для ID

**БЫЛО (упрощённая структура - НЕ РАБОТАЕТ):**
```tiscript
function render() {
    return
        <div .app>
            <div .left-pane>
                <div .simple-content>
                    <div .section-title>{translate('Your Desktop')}</div>
                    <div .id-section>
                        <span .id-label>ID: </span>
                        <span .id-value>{key_confirmed ? formatId(get_id()) : translate("Generating ...")}</span>
                    </div>
                    <div .status-section>{translate('Ready')}</div>
                    <div .footer-section>RustDesk by OpenFun 2026 ver. 2.0</div>
                </div>
            </div>
        </div>; 
}
```

**СТАЛО (оригинальная структура с input - РАБОТАЕТ):**
```tiscript
function render() {
    var is_can_screen_recording = handler.is_can_screen_recording(false);
    return
        <div .app>
            <div .left-pane>
                <div>
                    <div .title>{translate('Your Desktop')}</div>
                    <div .lighter-text style="display:none">{translate('desk_tip')}</div>
                    <div .your-desktop>
                        <div style="display:none"><MyIdMenu /></div>
                        <div .id-label-custom>ID</div>
                        {key_confirmed ? <input type="text" .readonly-text readonly value={formatId(get_id())}/> : <div .generating-text>{translate("Generating ...")}</div>}
                    </div>
                    <div .password style="display:none"><PasswordArea /></div>
                </div>
                {(!is_win || handler.is_installed() || disable_installation) ? "" : <div style="display:none"><InstallMe /></div>}
                {software_update_url && !disable_installation ? <div style="display:none"><UpdateMe /></div> : ""}
                {is_win && handler.is_installed() && !software_update_url && handler.is_installed_lower_version() && !disable_installation ? <div style="display:none"><UpgradeMe /></div> : ""}
                {is_can_screen_recording ? "": <div style="display:none"><CanScreenRecording /></div>}
                {is_can_screen_recording && !handler.is_process_trusted(false) ? <div style="display:none"><TrustMe /></div> : ""}
                {!service_stopped && is_can_screen_recording && handler.is_process_trusted(false) && handler.is_installed() && !handler.is_installed_daemon(false) ? <div style="display:none"><InstallDaemon /></div> : ""}
                {system_error ? <div style="display:none"><SystemError /></div> : ""}
                <ConnectStatus @{this.connect_status} />
            </div>
            <div .right-pane style="display:none">
                <div .right-content>
                    <div .card-connect>
                        <div .title>{translate('Control Remote Desktop')}</div>
                        <ID @{this.remote_id} />
                        <div .right-buttons>
                            <button .button .outline #file-transfer>{translate('Transfer file')}</button>
                            <button .button #connect>{translate('Connect')}</button>
                        </div>
                    </div>
                    <MultipleSessions @{this.multipleSessions} />
                </div>
            </div>
        </div>; 
}
```

**ПОЧЕМУ ВАЖНО:**
- `<input type="text">` **КРИТИЧНО** для обновления ID! RustDesk код ожидает input элемент.
- `<span>` **НЕ РАБОТАЕТ** - ID не будет обновляться!
- `<MyIdMenu />` скрыт через `display:none` - нет меню настроек сервера
- Все компоненты присутствуют но скрыты - правильная работа RustDesk

#### Изменение 1.2: Размер окна 320x200

**Найди строки 23-24:**
```tiscript
view.windowMinSize = (320, 200);
view.windowMaxSize = (320, 200);
```

**Убедись что они такие** (размер 320x200 пикселей)

---

### Файл 2: `src/ui/index.css`

#### Изменение 2.1: Tabler.io Dark Theme - фон и базовые цвета

**ДОБАВЬ в начало файла (после существующих правил):**
```css
/* === TABLER.IO DARK THEME === */
html, body {
    background: #182433;
    color: #f8fafc;
    overflow: hidden;
}

body {
    margin: 0;
    padding: 0;
}
```

#### Изменение 2.2: Скрыть правую панель

**ДОБАВЬ:**
```css
/* Скрыть правую панель (подключение к другим ПК) */
.right-pane {
    display: none !important;
}
```

#### Изменение 2.3: Стили для левой панели

**ДОБАВЬ:**
```css
/* Левая панель */
.left-pane {
    background: #182433;
    padding: 16px;
    width: 100%;
    height: 100%;
    box-sizing: border-box;
}

.left-pane > div:nth-child(1) {
    width: 100%;
}
```

#### Изменение 2.4: Заголовок "Ваш рабочий стол"

**ДОБАВЬ:**
```css
/* Заголовок */
.title {
    font-size: 1.1em;
    color: #f8fafc;
    margin-bottom: 12px;
    font-weight: 500;
}
```

#### Изменение 2.5: Стили для ID (КЛЮЧЕВОЕ!)

**НАЙДИ И ЗАМЕНИ:**
```css
/* БЫЛО: */
.your-desktop input[type=text] {
    padding: 0;
    border: none;
    height: 1.5em;
    background: transparent;
    color: #206bc4;
    font-weight: bold;
    font-size: 1.2em;
}

/* СТАЛО: */
.your-desktop input[type=text] {
    padding: 0;
    border: none;
    height: 1.8em;
    background: transparent;
    color: #206bc4;
    font-weight: bold;
    font-size: 1.5em;  /* Увеличено с 1.2em до 1.5em */
}
```

#### Изменение 2.6: Надпись "ID" и "Generating..."

**ДОБАВЬ:**
```css
/* Надпись "ID" */
.left-pane > div:nth-child(1) .id-label-custom {
    font-size: 1.1em;
    color: #cbd5e1;
    margin-bottom: 5px;
}

/* Текст "Generating..." */
.generating-text {
    font-weight: bold;
    color: #206bc4;
    font-size: 1.5em;
    background: transparent;
    padding: 8px 0;
}
```

#### Изменение 2.7: Статус "Готов" и footer

**ДОБАВЬ:**
```css
/* Статус подключения */
div.connect-status {
    color: #2fb344;
    font-size: 0.95em;
    margin-top: 12px;
}

/* Footer "RustDesk by OpenFun 2026 ver. 2.0" */
div.connect-status::after {
    content: "RustDesk by OpenFun 2026 ver. 2.0";
    display: block;
    font-size: 0.85em;
    color: #64748b;
    margin-top: 8px;
    text-align: left;
}
```

#### Изменение 2.8: Другие элементы в тёмной теме

**ДОБАВЬ:**
```css
/* Меню и иконки */
svg#menu, svg#edit {
    fill: #64748b;
}

svg#menu:hover, svg#edit:hover {
    fill: #94a3b8;
}

/* Сообщения установки */
.install-me {
    background: #1e293b;
    border: 1px solid #334155;
    color: #cbd5e1;
}

/* Trust Me блок */
.trust-me {
    background: #1e293b;
    border: 1px solid #334155;
    color: #cbd5e1;
}

/* Кнопки */
button.button {
    background: #206bc4;
    color: #ffffff;
    border: none;
}

button.button:hover {
    background: #1862ab;
}

button.button.outline {
    background: transparent;
    border: 1px solid #206bc4;
    color: #206bc4;
}

button.button.outline:hover {
    background: #206bc4;
    color: #ffffff;
}
```

---

## 🔒 Безопасность

### Файл 3: `src/common.rs`

#### Изменение 3.1: Приоритет ENV для сервера

**НАЙДИ функцию `get_custom_rendezvous_server` (примерно строка 1031):**

**БЫЛО (приоритет: EXE → Config → ENV):**
```rust
pub fn get_custom_rendezvous_server(custom: String) -> String {
    #[cfg(windows)]
    if let Ok(lic) = crate::platform::windows::get_license_from_exe_name() {
        if !lic.host.is_empty() {
            return lic.host.clone();
        }
    }
    if !custom.is_empty() {
        return custom;
    }
    if !config::PROD_RENDEZVOUS_SERVER.read().unwrap().is_empty() {
        return config::PROD_RENDEZVOUS_SERVER.read().unwrap().clone();
    }
    "".to_owned()
}
```

**СТАЛО (приоритет: ENV → EXE → Config):**
```rust
pub fn get_custom_rendezvous_server(custom: String) -> String {
    // ПРИОРИТЕТ 1: ENV переменная при билде (зашито, не изменить!)
    if !config::PROD_RENDEZVOUS_SERVER.read().unwrap().is_empty() {
        return config::PROD_RENDEZVOUS_SERVER.read().unwrap().clone();
    }
    // ПРИОРИТЕТ 2: Имя EXE файла (только если ENV не установлена)
    #[cfg(windows)]
    if let Ok(lic) = crate::platform::windows::get_license_from_exe_name() {
        if !lic.host.is_empty() {
            return lic.host.clone();
        }
    }
    // ПРИОРИТЕТ 3: Конфиг файл (только если ENV не установлена)
    if !custom.is_empty() {
        return custom;
    }
    "".to_owned()
}
```

#### Изменение 3.2: Приоритет ENV для API Server

**НАЙДИ функцию `get_api_server_` (примерно строка 1065):**

**БЫЛО:**
```rust
fn get_api_server_(api: String, custom: String) -> String {
    #[cfg(windows)]
    if let Ok(lic) = crate::platform::windows::get_license_from_exe_name() {
        if !lic.api.is_empty() {
            return lic.api.clone();
        }
    }
    if !api.is_empty() {
        return api.to_owned();
    }
    let api = option_env!("API_SERVER").unwrap_or_default();
    if !api.is_empty() {
        return api.into();
    }
    let s0 = get_custom_rendezvous_server(custom);
    if !s0.is_empty() {
        let s = crate::increase_port(&s0, -2);
        if s == s0 {
            return format!("http://{}:{}", s, config::RENDEZVOUS_PORT - 2);
        } else {
            return format!("http://{}", s);
        }
    }
    "https://admin.rustdesk.com".to_owned()
}
```

**СТАЛО:**
```rust
fn get_api_server_(api: String, custom: String) -> String {
    // ПРИОРИТЕТ 1: ENV переменная при билде (зашито, не изменить!)
    let api_env = option_env!("API_SERVER").unwrap_or_default();
    if !api_env.is_empty() {
        return api_env.into();
    }
    // ПРИОРИТЕТ 2: Имя EXE файла (только если ENV не установлена)
    #[cfg(windows)]
    if let Ok(lic) = crate::platform::windows::get_license_from_exe_name() {
        if !lic.api.is_empty() {
            return lic.api.clone();
        }
    }
    // ПРИОРИТЕТ 3: Конфиг файл (только если ENV не установлена)
    if !api.is_empty() {
        return api.to_owned();
    }
    let s0 = get_custom_rendezvous_server(custom);
    if !s0.is_empty() {
        let s = crate::increase_port(&s0, -2);
        if s == s0 {
            return format!("http://{}:{}", s, config::RENDEZVOUS_PORT - 2);
        } else {
            return format!("http://{}", s);
        }
    }
    "https://admin.rustdesk.com".to_owned()
}
```

#### Изменение 3.3: Приоритет ENV для ключа

**НАЙДИ функцию `get_key` (примерно строка 1526):**

**БЫЛО:**
```rust
pub async fn get_key(sync: bool) -> String {
    #[cfg(windows)]
    if let Ok(lic) = crate::platform::windows::get_license_from_exe_name() {
        if !lic.key.is_empty() {
            return lic.key;
        }
    }
    #[cfg(target_os = "ios")]
    let mut key = Config::get_option("key");
    #[cfg(not(target_os = "ios"))]
    let mut key = if sync {
        Config::get_option("key")
    } else {
        let mut options = crate::ipc::get_options_async().await;
        options.remove("key").unwrap_or_default()
    };
    if key.is_empty() {
        key = config::RS_PUB_KEY.to_owned();
    }
    key
}
```

**СТАЛО:**
```rust
pub async fn get_key(sync: bool) -> String {
    // ПРИОРИТЕТ 1: ENV переменная при билде (зашито, не изменить!)
    if !config::RS_PUB_KEY.is_empty() {
        return config::RS_PUB_KEY.to_owned();
    }
    // ПРИОРИТЕТ 2: Имя EXE файла (только если ENV не установлена)
    #[cfg(windows)]
    if let Ok(lic) = crate::platform::windows::get_license_from_exe_name() {
        if !lic.key.is_empty() {
            return lic.key;
        }
    }
    // ПРИОРИТЕТ 3: Конфиг файл (только если ENV не установлена)
    #[cfg(target_os = "ios")]
    let key = Config::get_option("key");
    #[cfg(not(target_os = "ios"))]
    let key = if sync {
        Config::get_option("key")
    } else {
        let mut options = crate::ipc::get_options_async().await;
        options.remove("key").unwrap_or_default()
    };
    key
}
```

---

### Файл 4: `src/ipc.rs` (Часть 1 - Настройки сервера)

#### Изменение 4.1: Блокировка сохранения одной опции

**НАЙДИ функцию `set_option` (примерно строка 1285):**

**БЫЛО:**
```rust
pub fn set_option(key: &str, value: &str) {
    let mut options = get_options();
    if value.is_empty() {
        options.remove(key);
    } else {
        options.insert(key.to_owned(), value.to_owned());
    }
    set_options(options).ok();
}
```

**СТАЛО:**
```rust
pub fn set_option(key: &str, value: &str) {
    // ЗАЩИТА: Запрещаем сохранение настроек сервера если они зашиты через ENV
    let is_server_setting = matches!(
        key,
        "custom-rendezvous-server" | "relay-server" | "api-server" | "key"
    );
    if is_server_setting {
        // Проверяем установлены ли ENV переменные
        let has_env_server = option_env!("RENDEZVOUS_SERVER").unwrap_or("").is_empty() == false;
        let has_env_key = config::RS_PUB_KEY.is_empty() == false;
        let has_env_api = option_env!("API_SERVER").unwrap_or("").is_empty() == false;
        
        if has_env_server || has_env_key || has_env_api {
            log::info!("Blocked attempt to save server setting '{}' - value is locked via build ENV", key);
            return; // НЕ сохраняем!
        }
    }
    
    let mut options = get_options();
    if value.is_empty() {
        options.remove(key);
    } else {
        options.insert(key.to_owned(), value.to_owned());
    }
    set_options(options).ok();
}
```

#### Изменение 4.2: Блокировка сохранения нескольких опций

**НАЙДИ функцию `set_options` (примерно строка 1296):**

**БЫЛО:**
```rust
#[tokio::main(flavor = "current_thread")]
pub async fn set_options(value: HashMap<String, String>) -> ResultType<()> {
    let _nat = CheckTestNatType::new();
    if let Ok(mut c) = connect(1000, "").await {
        c.send(&Data::Options(Some(value.clone()))).await?;
        // do not put below before connect, because we need to check should_exit
        c.next_timeout(1000).await.ok();
    }
    Config::set_options(value);
    Ok(())
}
```

**СТАЛО:**
```rust
#[tokio::main(flavor = "current_thread")]
pub async fn set_options(value: HashMap<String, String>) -> ResultType<()> {
    // ЗАЩИТА: Фильтруем настройки сервера если они зашиты через ENV
    let has_env_server = option_env!("RENDEZVOUS_SERVER").unwrap_or("").is_empty() == false;
    let has_env_key = config::RS_PUB_KEY.is_empty() == false;
    let has_env_api = option_env!("API_SERVER").unwrap_or("").is_empty() == false;
    
    let mut filtered_value = value.clone();
    if has_env_server || has_env_key || has_env_api {
        // Удаляем защищённые ключи из сохранения
        let blocked_keys = ["custom-rendezvous-server", "relay-server", "api-server", "key"];
        for key in blocked_keys {
            if filtered_value.remove(key).is_some() {
                log::info!("Filtered out server setting '{}' from save - value is locked via build ENV", key);
            }
        }
    }
    
    let _nat = CheckTestNatType::new();
    if let Ok(mut c) = connect(1000, "").await {
        c.send(&Data::Options(Some(filtered_value.clone()))).await?;
        // do not put below before connect, because we need to check should_exit
        c.next_timeout(1000).await.ok();
    }
    Config::set_options(filtered_value);
    Ok(())
}
```

---

### Файл 4: `src/ipc.rs` (Часть 2 - Статичный пароль)

#### Изменение 4.3: Приоритет ENV для пароля

**НАЙДИ функцию `get_permanent_password` (примерно строка 1145):**

**БЫЛО:**
```rust
pub fn get_permanent_password() -> String {
    if let Ok(Some(v)) = get_config("permanent-password") {
        Config::set_permanent_password(&v);
        v
    } else {
        Config::get_permanent_password()
    }
}
```

**СТАЛО:**
```rust
pub fn get_permanent_password() -> String {
    // ПРИОРИТЕТ 1: ENV переменная при билде (зашито, не изменить!)
    let env_password = option_env!("RS_PASSWORD").unwrap_or("");
    if !env_password.is_empty() {
        return env_password.to_owned();
    }
    // ПРИОРИТЕТ 2: Конфиг файл (только если ENV не установлена)
    if let Ok(Some(v)) = get_config("permanent-password") {
        Config::set_permanent_password(&v);
        v
    } else {
        Config::get_permanent_password()
    }
}
```

#### Изменение 4.4: Блокировка сохранения пароля

**НАЙДИ функцию `set_permanent_password` (примерно строка 1160):**

**БЫЛО:**
```rust
pub fn set_permanent_password(v: String) -> ResultType<()> {
    Config::set_permanent_password(&v);
    set_config("permanent-password", v)
}
```

**СТАЛО:**
```rust
pub fn set_permanent_password(v: String) -> ResultType<()> {
    // ЗАЩИТА: Запрещаем сохранение пароля если он зашит через ENV
    let env_password = option_env!("RS_PASSWORD").unwrap_or("");
    if !env_password.is_empty() {
        log::info!("Blocked attempt to save permanent password - value is locked via build ENV");
        return Ok(()); // НЕ сохраняем!
    }
    // Сохраняем только если ENV не установлена
    Config::set_permanent_password(&v);
    set_config("permanent-password", v)
}
```

---

## 🚀 GitHub Actions

### Файл 5: `.github/workflows/build-x86-sciter-only.yml`

#### Изменение 5.1: Добавление ENV переменных

**НАЙДИ секцию `- name: Build rustdesk x86 sciter` (примерно строка 85):**

**БЫЛО:**
```yaml
      - name: Build rustdesk x86 sciter
        id: build
        shell: bash
        run: |
          python3 res/inline-sciter.py
          # Patch sciter x86
          sed -i 's/branch = "dyn"/branch = "dyn_x86"/g' ./Cargo.toml
          cargo build --features inline,vram,hwcodec --release --bins
```

**СТАЛО:**
```yaml
      - name: Build rustdesk x86 sciter
        id: build
        shell: bash
        env:
          RENDEZVOUS_SERVER: ${{ secrets.RENDEZVOUS_SERVER }}
          RS_PUB_KEY: ${{ secrets.RS_PUB_KEY }}
          API_SERVER: ${{ secrets.API_SERVER }}
          RS_PASSWORD: ${{ secrets.RS_PASSWORD }}
        run: |
          python3 res/inline-sciter.py
          # Patch sciter x86
          sed -i 's/branch = "dyn"/branch = "dyn_x86"/g' ./Cargo.toml
          cargo build --features inline,vram,hwcodec --release --bins
```

---

## 🔑 Настройка GitHub Secrets

### Шаг 1: Открой настройки репозитория
1. Открой свой репозиторий на GitHub
2. Нажми **Settings** (⚙️)
3. В левом меню найди **Secrets and variables** → **Actions**
4. Нажми **New repository secret**

### Шаг 2: Добавь секреты

#### Secret 1: RENDEZVOUS_SERVER
```
Name: RENDEZVOUS_SERVER
Value: твой-сервер.com:21116
```
(замени `твой-сервер.com` на свой IP или домен)

#### Secret 2: RS_PUB_KEY
```
Name: RS_PUB_KEY
Value: [твой_публичный_ключ_в_base64]
```
(найди на сервере в файле `id_ed25519.pub` или в логах сервера)

#### Secret 3: API_SERVER
```
Name: API_SERVER
Value: https://твой-сервер.com
```
(или оставь пустым если не нужен)

#### Secret 4: RS_PASSWORD
```
Name: RS_PASSWORD
Value: твой_статичный_пароль
```
(например: `MySecurePass123`)

### Шаг 3: Проверь секреты
После добавления у тебя должно быть **4 секрета**:
- ✅ RENDEZVOUS_SERVER
- ✅ RS_PUB_KEY
- ✅ API_SERVER
- ✅ RS_PASSWORD

---

## 📦 Использование

### Сборка

1. **Залей изменённые файлы на GitHub:**
   ```bash
   git add src/ui/index.tis
   git add src/ui/index.css
   git add src/common.rs
   git add src/ipc.rs
   git add .github/workflows/build-x86-sciter-only.yml
   git commit -m "Кастомизация: UI Tabler.io + защита настроек сервера"
   git push
   ```

2. **Запусти workflow:**
   - Открой **Actions** → **Build Only x86 Sciter Version**
   - Нажми **Run workflow** → **Run workflow**

3. **Скачай собранный EXE:**
   - После завершения сборки скачай артефакт
   - Файл будет называться `rustdesk-1.4.5-x86-sciter.exe`

### Развёртывание для кассиров

1. **Просто скопируй `rustdesk-1.4.5-x86-sciter.exe` на компьютер кассира**

2. **Изменение ID (если нужно):**
   Создай файл `RustDesk.toml` рядом с `rustdesk.exe`:
   ```toml
   id = "123456789"
   ```
   (Пароль зашит в EXE через `RS_PASSWORD` secret)

3. **Запусти RustDesk:**
   - Двойной клик по exe
   - ID сгенерируется (или возьмётся из `RustDesk.toml`)
   - Подключение к твоему серверу!

---

## ✅ Проверка защиты

### Что кассир НЕ может сделать:

1. ❌ **Переименовать exe** в `rustdesk,host=другой-сервер.com.exe` - НЕ сработает (ENV приоритетнее)
2. ❌ **Изменить сервер через UI** - меню скрыто
3. ❌ **Посмотреть настройки сервера в `RustDesk.toml`** - их там нет (не сохраняются)
4. ❌ **Посмотреть настройки сервера в `RustDesk2.toml`** - их там нет (не сохраняются)

### Что кассир МОЖЕТ сделать:

1. ✅ **Изменить ID** в `RustDesk.toml`:
   ```toml
   id = "новый_id"
   ```

**Пароль изменить НЕЛЬЗЯ** - он зашит через `RS_PASSWORD` secret! 🔒

---

## 🎯 Итоговая схема защиты

```
┌─────────────────────────────────────────┐
│       Попытка изменить сервер           │
└─────────────────┬───────────────────────┘
                  │
                  ▼
    ┌─────────────────────────────┐
    │  Есть ENV переменная?       │
    │  (RENDEZVOUS_SERVER)        │
    └──────┬──────────────┬───────┘
           │ ДА           │ НЕТ
           ▼              ▼
    ┌─────────────┐  ┌──────────────┐
    │ Использовать│  │ Проверить имя│
    │ ENV (зашито)│  │ EXE файла    │
    │   🔒        │  │              │
    └─────────────┘  └──────┬───────┘
                            │
                            ▼
                     ┌─────────────┐
                     │ Проверить   │
                     │ Config файл │
                     └─────────────┘
```

**Приоритеты:**
1. 🔒 **ENV** (зашито в EXE) ← **ВЫСШИЙ ПРИОРИТЕТ**
2. 🔓 Имя EXE файла
3. 🔓 Config файл

---

## 📞 Поддержка

Если что-то не работает:

1. **ID не генерируется?**
   - Проверь что используешь `<input type="text">`, а НЕ `<span>` в `index.tis`
   - Проверь что все компоненты присутствуют (хоть и скрыты)

2. **Кассир может менять сервер?**
   - Проверь что GitHub Secrets установлены
   - Проверь что изменения в `src/common.rs` и `src/ipc.rs` применены
   - Пересобери через GitHub Actions

3. **Неправильные цвета?**
   - Проверь что все изменения в `src/ui/index.css` применены
   - Цвета Tabler.io: фон `#182433`, текст `#f8fafc`, синий `#206bc4`

---

**Создано:** 2026-01-15  
**Версия RustDesk:** 1.4.5  
**Архитектура:** x86 (32-bit) Sciter

🎨 **OpenFun 2026**

